# Create a virtual environment for the APIpython -m venv api_env
# Activate the virtual environment (Windows)
api_env\Scripts\activate
# Activate the virtual environment (Linux/Mac)# source api_env/bin/activate

# Install the API dependencies in the virtual environment




pip install fastapi uvicorn python-multipart
python -m spacy download en_core_web_sm
